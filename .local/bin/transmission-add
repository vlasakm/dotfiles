#!/bin/sh

# inspired by Luke Smith

if ! pgrep -x transmission-daemon; then
	transmission-daemon
	sleep 2
fi

transmission-remote -a "$1"

echo "Torrent added" "$(echo "$1" | cut -d\& -f2 | cut -d= -f2)"
notify-send "Torrent added" "$(echo "$1" | cut -d\& -f2 | cut -d= -f2)"
