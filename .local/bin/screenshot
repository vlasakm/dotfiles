#!/bin/sh

if [ "$1" = "-s" ]; then
	scrot_arg="-s"
else
	scrot_arg="--"
fi

filename="$(date "+%Y-%m-%d-%H%M%S.png")"

if scrot "$scrot_arg" "$HOME/media/screenshots/$filename"; then
	notify-send "Screenshot saved" "$filename"
fi
